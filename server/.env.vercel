# Environment
# Set to 'production' for Vercel deployment
NODE_ENV=production

# MongoDB Atlas connection string - CRITICAL
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
# Troubleshooting:
# 1. Make sure username and password are URL-encoded (special characters like @ need to be encoded)
# 2. Verify the database name exists in your Atlas cluster
# 3. Ensure Network Access in Atlas allows connections from 0.0.0.0/0
MONGODB_URI=mongodb+srv://prabhathdilshan2001:1234@as.gp7z1.mongodb.net/dt_vehicles_management

# Frontend URL for CORS - CRITICAL
# This should be your exact Vercel frontend URL
# Example: https://dt-vehicles-client.vercel.app
FRONTEND_URL=https://dt-vehicles-client.vercel.app

# Cloudinary configuration (if using image upload)
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret

# MongoDB Connection Troubleshooting:
# - If you're getting 500 errors, verify these settings in MongoDB Atlas:
# 1. User has "Read and Write to any database" permissions
# 2. Network Access has 0.0.0.0/0 in the IP Access List
# 3. Database exists in the cluster
