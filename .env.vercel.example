# Environment Variables for Vercel Deployment
# Copy these to your Vercel project settings

# MongoDB Database
MONGODB_URI=mongodb+srv://your-username:your-password@cluster.mongodb.net/vehicles-db?retryWrites=true&w=majority

# Optional: Database URL (alternative name)
DATABASE_URL=mongodb+srv://your-username:your-password@cluster.mongodb.net/vehicles-db?retryWrites=true&w=majority

# Application Environment
NODE_ENV=production

# Optional: JWT Secret (if authentication is added later)
JWT_SECRET=your-super-secret-jwt-key-here

# Optional: Email Configuration (for notifications)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_SERVICE=gmail

# Optional: File Upload Configuration
MAX_FILE_SIZE=5000000
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,application/pdf
